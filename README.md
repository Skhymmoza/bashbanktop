# ⚜ БАШБАНК (BASHBANK)
Веб-приложение "Башкирский Банк" с плоской файловой структурой (Flat File Structure) для самого простого деплоя.

## 📁 Структура
В проекте нет папок. Все файлы лежат в одной куче:
1. `index.html` - Интерфейс
2. `style.css` - Стили (цвета флага Башкортостана)
3. `app.js` - Логика фронтенда
4. `api.php` - Весь бэкенд (настройки БД + роутер + API)
5. `database.sql` - Дамп базы данных

## 🚀 Установка за 3 минуты
1. **Залейте файлы:** Загрузите все 5 файлов в папку `public_html` на вашем хостинге.
2. **База данных:** Создайте базу MySQL и импортируйте в неё `database.sql`.
3. **Конфиг:** Откройте `api.php` и впишите свои данные от базы в первых строчках:
   ```php
   define('DB_NAME', 'ваша_база');
   define('DB_USER', 'ваш_юзер');
   define('DB_PASS', 'ваш_пароль');
